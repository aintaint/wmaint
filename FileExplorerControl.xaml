<UserControl x:Class="wmaint.FileExplorerControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">
    
    
    <StackPanel HorizontalAlignment="Center">

        <!--UserControl x:Class=""
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Behaviors="clr-namespace:wmaint.Behaviors"
             xmlns:local="clr-namespace:wmaint"
             xmlns:resx="clr-namespace:wmaint.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"    
             DataContext="{Binding Main, Source={StaticResource Locator}}" -->

        <!-- FAMILY TREE -->
        <TreeView x:Name="fileTreeView" ItemsSource="{Binding FirstGeneration}" >

        </TreeView>

    </StackPanel>

</UserControl>
